const chalk = require('chalk');

module.exports = async function installClaudeCommand() {
  console.log(chalk.blue('Installing Claude integration...'));
  console.log(chalk.yellow('\nThis command is not yet implemented.'));
};